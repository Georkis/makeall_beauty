{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <div class="page-header">
        <h4 class="page-title">Comment</h4>
        <ul class="breadcrumbs">
            <li class="nav-home">
                <a href="{{ path('dashboard') }}">
                    <i class="flaticon-home"></i>
                </a>
            </li>
            <li class="separator">
                <i class="flaticon-right-arrow"></i>
            </li>
            <li class="nav-home">
                <a href="{{ path('comment_index') }}">
                    Comment                </a>
            </li>
            <li class="separator">
                <i class="flaticon-right-arrow"></i>
            </li>
            <li class="nav-item">
                <a href="#">Mostrar </a>
            </li>
        </ul>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title">Mostrar</h5>
                    <div class="card-tools float-right">
                        <a href="{{ path('comment_index') }}" class="btn btn-secondary">Regresar</a>
                        <a href="{{ path('comment_edit', {'id': comment.id}) }}" class="btn btn-success"><i class="fa fa-edit"></i> Editar</a>
                    </div>
                    <div class="card-body">
                        <table class="table">
                            <tbody>
                                                            <tr>
                                    <th>Id</th>
                                    <td>{{ comment.id }}</td>
                                </tr>
                                                            <tr>
                                    <th>Description</th>
                                    <td>{{ comment.description }}</td>
                                </tr>
                                                        </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
<!--    {{ include('--><?//= $route_name ?><!--/_delete_form.html.twig') }}-->
{% endblock %}
